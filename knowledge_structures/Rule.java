package knowledge;

public class Rule extends AbstractEntity {
	
	/*
	 * Corresponds with a first-order logic formula. Predicates can be Events or just pairs of Logos and Adjectives etc.
	 * 
	 * Examples of Rules:
	 * IF (I lose a game) THEN (I can play the game again)
	 * IF (apple has color X) THEN (apple's surface is mostly of the color X)
	 * IF (human issues a command to the robot) THEN (the robot to whom the command was addressed must obey)
	 */

}
